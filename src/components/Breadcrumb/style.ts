import styled from 'styled-components';
import { NavLink } from 'react-router-dom';

export const BCLink = styled(NavLink)`
	color: inherit;
	display: inline-block;
	font-size: 1.125rem;
	margin: 2rem 0;
`;

export const Separator = styled.span`
	margin: 0 .625rem;
`;
